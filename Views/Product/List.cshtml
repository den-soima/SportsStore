@model ProductListViewModel

@foreach (var p in Model.Products)
{
   @Html.Partial("ProductSummary", p)
}

<nav aria-label="HtmlTagHelper pagination" class="float-right">
    <ul class="pagination" page-model="@Model.PagingInfo" page-action="List"></ul>
</nav>